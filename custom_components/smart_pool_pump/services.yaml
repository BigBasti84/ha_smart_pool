set_season_mode:
  name: Set season mode
  description: Set Smart Pool season mode.
  fields:
    season:
      required: true
      selector:
        select:
          options:
            - winter
            - summer

recalculate_now:
  name: Recalculate now
  description: Force next tick to rebuild slot schedule.

run_now:
  name: Run check now
  description: Trigger an immediate pump schedule evaluation.
